# Archive — native PE 預設路徑（soft reset）

自 **2026-08-07 Wine pivot（NTW0）** 起，產品預設 `execution_backend=wine`／`engine=proton-ge`。

本目錄標記**歷史／legacy** 自研 native PE 預設路線：

- 程式與證據**不刪除**（保留 git 歷史與 `tests/**/output`）。
- 研究／回歸可用：`STRAWNT_LEGACY_NATIVE=1`（**unsupported**）。
- 盤點清單：`tests/archive/native/README.md`。

相關 ADR：`docs/decisions/2026-08-07-wine-pivot.md`。
