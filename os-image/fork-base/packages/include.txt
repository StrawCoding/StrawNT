# Extra packages to ensure in fork base (one per line)
# Curated fork-f2-manifest-repo — firmware, flatpak stack, essentials
linux-firmware
firmware-sof-signed
flatpak
gnome-software-plugin-flatpak
ca-certificates
curl
wget
