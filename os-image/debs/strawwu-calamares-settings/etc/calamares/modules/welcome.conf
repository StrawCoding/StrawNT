# Welcome — no storage gate (QEMU blank virtio disk has no partitions yet).
---
showSupportUrl: true
showKnownIssuesUrl: true
showReleaseNotesUrl: true

requirements:
  requiredRam: 0.5
  # C8: minimum free space for replace/alongside dual-boot paths (GiB).
  requiredStorage: 8
  check:
    - ram
    - storage
  required:
    - ram
    - storage

geoip:
  style: none
