overwrite: false
defaults:
    GRUB_ENABLE_CRYPTODISK: true
    GRUB_DISTRIBUTOR: "StrawWU"
    # C8: detect Windows/macOS/other Linux for dual-boot menu (disabled in install-e2e).
    GRUB_DISABLE_OS_PROBER: false
