# StrawWU target identity (W5-I3) — LEG3 GRUB menu title
GRUB_DISTRIBUTOR="StrawWU"
# Physical display: Plymouth + fbcon on real monitor (not serial/QEMU only)
GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT} console=tty0"
