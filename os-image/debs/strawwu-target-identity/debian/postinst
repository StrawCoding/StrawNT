#!/bin/sh
set -e
if [ "$1" = "configure" ] && [ -x /usr/bin/strawwu-target-identity ]; then
    if [ -d /boot ] && command -v update-grub >/dev/null 2>&1; then
        /usr/bin/strawwu-target-identity apply --skip-initramfs 2>/dev/null || true
    fi
fi
