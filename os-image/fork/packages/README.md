# Fork upstream package overlays

Place forked Ubuntu package source trees here (debian/ + upstream tarball).

Each subdirectory is one package, e.g.:

```
os-image/fork/packages/gnome-shell/
os-image/fork/packages/mutter/
```

Built debs publish to strawwu-fork APT suite (fork-f5).
