
## [2026-07-03T17:28-04:00] HERMES SUPERVISOR — v22 根因與修正（已完成，勿再改 install-guest.sh）

v22 FAIL 根因：`send_advance_key()` 中 `xdotool windowactivate --sync "${wid}"` 在 Calamares modal 確認對話框覆蓋父視窗時無限掛死。

**已由 Supervisor 修正 install-guest.sh**（9p 共享，免 rebuild）：
1. `windowactivate --sync` → `timeout 3 xdotool windowactivate`（防掛死）
2. `windowfocus --sync` → `timeout 3 xdotool windowfocus`（同上）
3. 每次 send_advance_key 結尾加 `xdotool key Return`（送到 focused 視窗=確認對話框，確認安裝）

**你的下一步**：
- **不要修改** install-guest.sh — 修正已就位
- 清理 artifacts（lock/result/logs）
- 直接跑 `make test-install-e2e`（timeout 3600s）
- 等待結果，不要 rebuild ISO

