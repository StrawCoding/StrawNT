[package]
name = "strawwu-graphics"
version = "0.3.0"
edition = "2021"
description = "StrawWU graphics stack — DXGI→Vulkan, wgl→GLX/EGL, D3D11→VK translation stubs"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
