[package]
name = "strawwu-nt"
version.workspace = true
edition.workspace = true
description = "StrawWU Windows NT compatibility layer — TEB/PEB, PE loader, Win32 API stubs"
license.workspace = true
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
strawwu-bridge = { path = "../strawwu-bridge" }

[dev-dependencies]
