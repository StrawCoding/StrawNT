[package]
name = "strawwu-bridge"
version = "0.3.0"
edition = "2021"
description = "StrawWU kernel↔userspace IPC bridge — seccomp/policy layer"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
