[package]
name = "strawwu-bridge"
version.workspace = true
edition.workspace = true
description = "StrawWU kernel↔userspace IPC bridge — seccomp/policy layer"
license.workspace = true
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
