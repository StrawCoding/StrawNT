[package]
name = "strawwu-audio"
version.workspace = true
edition.workspace = true
description = "StrawWU audio/input — WASAPI→PipeWire bridge, XInput/DirectInput controller mapping"
license.workspace = true
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[[bin]]
name = "gx-audio-verify"
path = "src/bin/gx_audio_verify.rs"

[dev-dependencies]
