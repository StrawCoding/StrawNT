[package]
name = "strawwu-cli"
version = "0.4.0"
edition = "2021"
description = "StrawWU CLI helpers — devices list and rootfs integration"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
strawwu-device-proxy = { path = "../strawwu-device-proxy" }

[dev-dependencies]
tempfile = "3"
