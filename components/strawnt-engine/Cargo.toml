[package]
name = "strawnt-engine"
version.workspace = true
edition.workspace = true
description = "StrawNT Proton-GE / Wine engine: pin, doctor, runner, prefix, recipes, matrix"
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
