[package]
name = "strawwu-device-proxy"
version.workspace = true
edition.workspace = true
description = "StrawWU device driver proxy — udev/COM/CUPS/HID/IOCTL translation layer"
license.workspace = true
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
